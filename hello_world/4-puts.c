#include <stdio.h>

/**
 * main - this fuction prits
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char Mystr[] = "\"Programming is like building a multilingual puzzle";
puts(Mystr);
return (0);
}
